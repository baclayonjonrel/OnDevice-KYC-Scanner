platform :ios, '17.0'

target 'OnDevice-KYC-Scanner' do
  use_frameworks!

  # LiteRT is the current branding for TensorFlow Lite. The official iOS Swift
  # runtime is still distributed as the TensorFlowLiteSwift CocoaPod.
  pod 'TensorFlowLiteSwift', '~> 2.17'
end
